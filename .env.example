# API Configuration
# Copy this file to .env.local and update with your actual values

# Your API Gateway endpoint URL from the backend deployment
# Find this in CloudFormation outputs: ServerlessBackendStack -> ApiEndpoint
# or run: aws cloudformation describe-stacks --stack-name ServerlessBackendStack --profile FakeGods --query "Stacks[0].Outputs[?OutputKey=='ApiEndpoint'].OutputValue" --output text
REACT_APP_API_ENDPOINT=https://z4v1ognu3l.execute-api.eu-central-1.amazonaws.com/api/
